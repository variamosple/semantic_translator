services:
  semantic_translator:
    build:
      context: .
    image: semantic_translator
    container_name: semantic_translator
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped